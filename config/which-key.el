;;; ========================================================
;;; which-key configuration
;;; ========================================================

(leaf which-key
  :ensure t
  :config
  (which-key-mode)
  (setq which-key-idle-delay 0.5))
